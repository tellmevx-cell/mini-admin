x-logging: &default-logging
  driver: json-file
  options:
    max-size: "20m"
    max-file: "3"

services:
  mysql:
    image: ${MINIADMIN_MYSQL_IMAGE:-mysql:8.4}
    container_name: mini-admin-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MINIADMIN_MYSQL_DATABASE:-mini_admin}
      MYSQL_USER: ${MINIADMIN_MYSQL_USER:-miniadmin}
      MYSQL_PASSWORD: ${MINIADMIN_MYSQL_PASSWORD:?Set MINIADMIN_MYSQL_PASSWORD in .env}
      MYSQL_ROOT_PASSWORD: ${MINIADMIN_MYSQL_ROOT_PASSWORD:?Set MINIADMIN_MYSQL_ROOT_PASSWORD in .env}
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_0900_ai_ci
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u root -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 10s
      timeout: 5s
      retries: 18
      start_period: 40s
    logging: *default-logging

  redis:
    image: ${MINIADMIN_REDIS_IMAGE:-redis:7.4-alpine}
    container_name: mini-admin-redis
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: ${MINIADMIN_REDIS_PASSWORD:?Set MINIADMIN_REDIS_PASSWORD in .env}
    command: ["sh", "-c", "redis-server --appendonly yes --requirepass \"$${REDIS_PASSWORD}\""]
    volumes:
      - redis_data:/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$${REDIS_PASSWORD}\" ping | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 18
      start_period: 15s
    logging: *default-logging

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
      args:
        DOTNET_SDK_IMAGE: ${MINIADMIN_DOTNET_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:10.0}
        DOTNET_ASPNET_IMAGE: ${MINIADMIN_DOTNET_ASPNET_IMAGE:-mcr.microsoft.com/dotnet/aspnet:10.0}
    image: mini-admin-api:local
    container_name: mini-admin-api
    restart: unless-stopped
    init: true
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ASPNETCORE_FORWARDEDHEADERS_ENABLED: "true"
      Jwt__SigningKey: ${MINIADMIN_JWT_SIGNING_KEY:?Set MINIADMIN_JWT_SIGNING_KEY in .env}
      Database__Provider: MySql
      Database__InitializeOnStartup: "true"
      Database__SchemaManagement: Auto
      ConnectionStrings__MiniAdmin: Server=mysql;Port=3306;Database=${MINIADMIN_MYSQL_DATABASE:-mini_admin};User=${MINIADMIN_MYSQL_USER:-miniadmin};Password=${MINIADMIN_MYSQL_PASSWORD};CharSet=utf8mb4;SslMode=None;AllowPublicKeyRetrieval=True;
      Cache__Provider: Redis
      Cache__Redis__Configuration: redis:6379,password=${MINIADMIN_REDIS_PASSWORD},abortConnect=False,defaultDatabase=0
      FileStorage__Provider: Local
      FileStorage__Local__RootPath: /app/storage/uploads
      Logging__LogLevel__Microsoft.EntityFrameworkCore.Database.Command: Warning
    ports:
      - "${MINIADMIN_HTTP_PORT:-127.0.0.1:8080}:8080"
    volumes:
      - uploads:/app/storage/uploads
    networks:
      - backend
      - edge
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent --show-error http://127.0.0.1:8080/health >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 18
      start_period: 180s
    stop_grace_period: 30s
    logging: *default-logging

  gateway:
    build:
      context: .
      dockerfile: Dockerfile.gateway
      args:
        DOTNET_SDK_IMAGE: ${MINIADMIN_DOTNET_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:10.0}
        DOTNET_ASPNET_IMAGE: ${MINIADMIN_DOTNET_ASPNET_IMAGE:-mcr.microsoft.com/dotnet/aspnet:10.0}
    image: mini-admin-gateway:local
    container_name: mini-admin-gateway
    restart: unless-stopped
    init: true
    depends_on:
      api:
        condition: service_healthy
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ASPNETCORE_FORWARDEDHEADERS_ENABLED: "true"
      ReverseProxy__Clusters__miniadmin_api__Destinations__api__Address: http://api:8080/
      RateLimiting__Enabled: "${MINIADMIN_GATEWAY_RATE_LIMITING_ENABLED:-true}"
      RateLimiting__PermitLimit: "${MINIADMIN_GATEWAY_RATE_LIMITING_PERMIT_LIMIT:-1200}"
      RateLimiting__WindowSeconds: "${MINIADMIN_GATEWAY_RATE_LIMITING_WINDOW_SECONDS:-60}"
      RateLimiting__LoginPermitLimit: "${MINIADMIN_GATEWAY_LOGIN_RATE_LIMITING_PERMIT_LIMIT:-20}"
      RateLimiting__LoginWindowSeconds: "${MINIADMIN_GATEWAY_LOGIN_RATE_LIMITING_WINDOW_SECONDS:-60}"
    ports:
      - "${MINIADMIN_GATEWAY_PORT:-127.0.0.1:8088}:8080"
    networks:
      - edge
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent --show-error http://127.0.0.1:8080/health >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    stop_grace_period: 20s
    logging: *default-logging

  web:
    build:
      context: frontend/vue-vben-admin
      dockerfile: apps/web-antd/Dockerfile
      args:
        NODE_IMAGE: ${MINIADMIN_NODE_IMAGE:-node:24-alpine}
        NGINX_IMAGE: ${MINIADMIN_NGINX_IMAGE:-nginx:1.27-alpine}
        VITE_GLOB_API_URL: /api
        NPM_REGISTRY: ${MINIADMIN_NPM_REGISTRY:-https://registry.npmmirror.com}
        PNPM_FETCH_TIMEOUT: ${MINIADMIN_PNPM_FETCH_TIMEOUT:-600000}
        PNPM_FETCH_RETRIES: ${MINIADMIN_PNPM_FETCH_RETRIES:-5}
        PNPM_NETWORK_CONCURRENCY: ${MINIADMIN_PNPM_NETWORK_CONCURRENCY:-4}
    image: mini-admin-web:local
    container_name: mini-admin-web
    restart: unless-stopped
    init: true
    depends_on:
      gateway:
        condition: service_healthy
    ports:
      - "${MINIADMIN_WEB_PORT:-5666}:80"
    networks:
      - edge
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --spider http://127.0.0.1/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    stop_grace_period: 15s
    logging: *default-logging

networks:
  backend:
    name: miniadmin_backend
    internal: true
  edge:
    name: miniadmin_edge

volumes:
  mysql_data:
    name: ${MINIADMIN_MYSQL_VOLUME:-miniadmin_mysql}
  redis_data:
    name: ${MINIADMIN_REDIS_VOLUME:-miniadmin_redis}
  uploads:
    name: ${MINIADMIN_UPLOADS_VOLUME:-miniadmin_uploads}
