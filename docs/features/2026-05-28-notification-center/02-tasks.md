# 通知中心任务执行文档

## 任务拆分

- [x] 扩展当前用户通知查询参数，支持分页和筛选。
- [x] 调整通知仓储查询逻辑，保持未读总数独立统计。
- [x] 增加 `通知中心` 菜单、权限和 admin 默认授权。
- [x] 增加 Vben 通知中心 API 参数类型。
- [x] 新增 Vben 通知中心页面。
- [x] 将顶部通知“查看全部”跳转到通知中心。
- [x] 补充测试和总结文档。

## TDD 计划

1. 后端测试先验证 `/notification/my` 的分页、已读状态筛选和分类筛选。
2. 菜单测试先验证 `SystemMonitor` 下存在 `NotificationCenter`。
3. 红灯确认后实现后端查询和菜单种子。
4. 后端绿灯后实现前端页面。
5. 最后跑后端测试、后端构建、前端构建，并做一次本地接口烟测。

## 实现注意

- 通知 API 仍只要求登录，不额外要求菜单权限；菜单权限用于控制页面入口。
- 顶部通知继续使用 `take=20` 的轻量调用。
- 通知中心页面使用 `page/pageSize` 的分页调用。
- 清空通知是当前用户维度，不影响其他用户。
