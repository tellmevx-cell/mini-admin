# 字典管理完工总结

## 完成内容

- 完成字典类型和字典项维护。
- 页面调整为符合后台习惯的左右布局。
- 修复点击字典类型后右侧数据不变化的问题。
- 支持给用户状态等字典增加子项。

## 关键实现

- `DictionaryType` 保存字典分类。
- `DictionaryItem` 保存分类下的值。
- 前端维护当前选中的 `typeId`，右侧请求始终带上该类型。

## 后续建议

- 增加按字典编码缓存读取接口。
- 将用户状态、通知类型等表单选项逐步改为读取字典。
- 增加字典项颜色、标签样式配置。

