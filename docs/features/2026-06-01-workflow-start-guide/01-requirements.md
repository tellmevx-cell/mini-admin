# 审批发起示例引导需求文档

## 背景

审批中心已经支持流程定义、条件分支和发起审批，但用户在发起时仍需要理解流程、业务标识、标题和表单 JSON 的关系。如果流程定义里的审批节点未配置审批人，发起时会提示“没有可用审批人”，需要在页面内给出更明确的引导。

## 目标

- 在“发起审批”页面提供示例引导入口。
- 说明创建和发起审批请求的关键步骤。
- 支持一键填入常见请假审批示例。
- 告知“没有可用审批人”的常见原因和检查项。

## 范围

- 本阶段做页面内轻量引导，不引入复杂新手任务系统。
- 不改变后端接口。
- 不改变现有流程定义和发起审批数据结构。
