# 租户到期时间与登录选项防呆任务

## 任务清单

- [x] 复现问题：将启用租户的到期时间设置为过去，验证登录选项不展示。
- [x] 补后端回归测试：过期启用租户在租户列表应显示为 `Expired`。
- [x] 补后端回归测试：新增租户不能提交过去的到期时间。
- [x] 补后端回归测试：编辑租户不能提交过去的到期时间。
- [x] 后端租户列表按有效状态返回和筛选。
- [x] 后端新增/编辑租户时校验到期时间。
- [x] 前端 DatePicker 禁用过去日期，并在提交时校验具体时间。
- [x] 运行回归测试验证修复。

## 数据流

```mermaid
flowchart LR
    A["平台新增/编辑租户"] --> B["前端提交前校验 ExpireAt"]
    B --> C["后端租户仓储校验 ExpireAt"]
    C --> D["保存租户"]
    D --> E["租户列表计算有效状态"]
    D --> F["登录页租户选项过滤未过期租户"]
```
