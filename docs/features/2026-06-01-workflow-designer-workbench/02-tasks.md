# 工作流设计器工作台任务执行文档

## 任务拆解

- [x] 使用 `critique` 思路审查现有页面问题。
- [x] 保留现有审批中心主导航，不破坏待办、发起、实例、已办。
- [x] 将流程定义从表格加弹窗改为工作台布局。
- [x] 增加左侧流程库，支持搜索、状态筛选、分页切换和选择编辑。
- [x] 增加中间大画布，保留拖拽节点、拖拽连线、整理连线。
- [x] 增加右侧流程属性和节点属性面板。
- [x] 用 Vue Flow 插槽实现开始、审批、条件、抄送、结束节点的自定义视觉。
- [x] 保存时继续提交 `designerJson` 和审批节点列表。
- [x] 明确条件、抄送节点当前为设计态节点。
- [x] 执行前端构建验证。

## 关键实现点

- 文件：
  - `frontend/vue-vben-admin/apps/web-antd/src/views/workflow/center/index.vue`
- 前端画布：
  - 使用 `@vue-flow/core`。
  - 使用 `Handle`、`Position` 和自定义节点插槽。
- 后端兼容：
  - 仅审批节点写入 `SaveWorkflowNodeRequest`。
  - 条件、抄送、结束等设计节点写入 `DesignerJson`。
  - 现有后端执行逻辑不需要改变。

## 风险控制

- 不改变 API 契约，避免影响现有审批闭环。
- 不把条件节点伪装成已可执行，避免用户误判。
- 不在本阶段引入第三方工作流引擎，避免学习成本和迁移风险突然扩大。
