# 工作流业务绑定配置任务执行文档

## 任务清单

- [x] 新增 `WorkflowBusinessBinding` 领域实体。
- [x] 扩展 `MiniAdminDbContext`，增加 DbSet、索引、字段映射和关联关系。
- [x] 扩展 MySQL 初始化逻辑，创建 `mini_workflow_business_bindings` 表。
- [x] 扩展工作流 DTO 和服务接口，增加业务绑定查询、保存、删除、按业务类型解析流程定义能力。
- [x] 实现仓储逻辑：租户隔离、重复业务类型校验、已发布流程校验、启停用。
- [x] 增加后端测试：重复业务类型拒绝、草稿流程不能绑定、停用绑定不能解析。
- [x] 增加 API：业务绑定列表、新增、编辑、删除、按业务类型解析。
- [x] 调整前端 API 类型和请求方法。
- [x] 在审批中心增加“业务绑定”页签，支持列表、表单、启停用和流程版本选择。
- [x] 运行后端测试、后端构建、前端构建。
- [x] 启动前后端并验证接口。
- [x] 补充总结文档。

## 第一版交互

- 列表展示：业务名称、业务类型、绑定流程、流程版本、发布状态、启用状态、更新时间。
- 表单字段：业务类型、业务名称、流程定义、是否启用、说明。
- 流程下拉：使用已发布流程选项，展示 `流程名称 v版本（编码）`。

## 后续扩展预留

- 代码生成器可以读取业务类型绑定，生成提交审批逻辑。
- 业务模块可以按 `BusinessType` 查询绑定流程，减少硬编码。
- 未来可以扩展“按条件选择流程”，但当前先保持一业务类型一流程。
